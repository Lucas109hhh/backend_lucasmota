let base = 20;
let altura = 50;

let area = base * altura;
let perimetro = 2 * (base + altura);

console.log(`A area do retangulo é ${area}.`);
console.log(`O perímetro do retangulo é ${perimetro}.`);